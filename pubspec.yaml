name: supabasechat
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  clipboard: ^0.1.3
  flutter:
    sdk: flutter
  flutter_bloc: ^7.1.0
  shared_preferences: ^2.0.7
  supabase_flutter: ^0.2.4
  timeago: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.6.0

flutter:
  uses-material-design: true
