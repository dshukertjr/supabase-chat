name: supabasechat
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.9.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  supabase: ^0.0.1-dev.8
  # supabase:
  #   path: ../supabase-dart

dev_dependencies:
  lint: ^1.3.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
